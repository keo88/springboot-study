spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: user
    password:
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create

logging:
  level:
    org:
      hibernate:
        SQL: debug
        orm.jdbc.bind: trace