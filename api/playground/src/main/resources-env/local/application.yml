spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: user
    password:
    driver-class-name: org.postgresql.Driver

logging:
  level:
    org:
      hibernate:
      SQL: debug
      descriptor:
        sql: trace